{
  "name": "@radioboi/worker",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker: Durable Objects (GameRoomArbitrator) + KV",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "bun test tests",
    "type-check": "tsc --noEmit",
    "lint": "biome lint ./src",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@msgpack/msgpack": "latest",
    "@radioboi/game-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
